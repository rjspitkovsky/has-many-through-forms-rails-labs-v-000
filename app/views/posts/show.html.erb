<%= @post.title %>
<%= @post.content %>

<% @post.comments.each do |comment| %>
  <%= comment.content %>
  <% end %>

  <% @unique_users.each do |user| %>
  <%= link_to user.username, user_path(user) %> 
  <% end %>
